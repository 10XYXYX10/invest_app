# 円ベース割安度シグナル アプリ 仕様書

自分専用の投資判断支援アプリ。各投資対象の「ドル建て下落率」と「ドル円の乖離」を合成した独自指数を監視し、しきい値を下回ったら買いシグナルを表示する。

## 1. コア指数の定義

- **X(下落率)** = (現在のドル建て価格 − 全期間最高値) ÷ 全期間最高値 × 100
  - 高値から下げているとき負の値になる(例: 10%暴落 → X = −10)
- **Y(為替乖離)** = (現在の為替レート − 基準値) ÷ 基準値 × 100
  - 中央値からの乖離。円安で正(例: ドル円160円 → Y = +6.67)。基準値は設定ファイルで定数管理し、将来変更可能にする
  - **通貨ペアは銘柄ごとに選ぶ**(`lib/config.ts` の `FX_PAIRS` / `AssetConfig.fxPairId`)
    - ドル円 `USDJPY=X` … 基準 150(既定)
    - ルピー円 `INRJPY=X` … 基準 1.65。インド Nifty50 のみ。過去10年(月足)で概ね 1.4〜1.9 のレンジに収まるため、その中央値(`dev/ルピーVS円.md`)
- **円ベース割安度指数** = X + Y
- **買いシグナル判定**: 指数 ≤ 対象ごとのしきい値

計算例: 全世界株式が10%暴落(X = −10)、ドル円160円(Y = +6.67)→ 指数 = −3.33 ≤ 0 → 買いシグナル点灯。

※ Xは必ず**ドル建て価格**で計算する。円建て価格を使うと為替が二重カウントになるため。

## 2. 投資対象

| 対象 | 使用ティッカー(代理) | 買いしきい値 | リスク度 |
|---|---|---|---|
| 全世界株式(オルカン) | ACWI | 0 以下 | 低 |
| NIFTY50 | INDY | **−10 以下** | 中(厳しめ) |
| 半導体ETF | SMH | **−20 以下** | 強(さらに厳しめ) |
| 米国総合債券 | AGG | 0 以下 | 低 |
| 為替 | USDJPY=X | — | — |

- しきい値は「−10以下」「−20以下」(より深い下落を要求する厳しい条件)であることに注意
- ~~NIFTY50は本来ルピー建てだが、米国上場ETF(INDY)を代理に使い、ドル建て+ドル円の枠組みに統一する~~
  - **改訂**: INDY ではなく円建ての投信(楽天・インド株Nifty50)と東証ETF(201A)を採用したため、この代理は不要になった。
    Nifty50 の為替乖離 Y は**ルピー円(基準 1.65)**で直接測る。組入資産が実質どの通貨で動くかに合わせるため。
- 銘柄・しきい値は設定ファイル(定数)で管理し、コード修正で追加・変更できれば十分(管理UIは不要)

## 3. 画面仕様

### 3.1 ダッシュボード(トップ)

各投資対象をカード形式で一覧表示。カードごとに:

- 過去最高値(全期間、ドル建て。円換算値も併記)
- 現在レート(ドル建て+円換算)
- 高値からの下落率 X(%)
- 為替乖離 Y(%)は共通ヘッダー等に現在のドル円とあわせて表示
- 円ベース割安度指数(X + Y)
- しきい値(推奨値)
- シグナル状態バッジ(買いシグナル点灯 / 非点灯を色で明示)
- 「チャート表示」ボタン

### 3.2 チャート表示

- 表示価格は**円換算**(ドル建て価格 × ドル円レート)
- **ライン / ローソク足 切り替え**
- **足種切り替え: 日足 / 週足 / 月足 / 年足**(表示期間の切替ではなく足の単位)
  - 年足はデータソースにないため日足or月足データから集計して生成する
  - 円換算ローソク足のOHLCは「USDのOHLC × その日の終値レート」による近似とする(高値・安値の厳密な円建て値は算出不能なため)
- ライブラリ候補: TradingView lightweight-charts(ローソク足対応・軽量)

## 4. データ取得

- データソース: Yahoo Finance(`yahoo-finance2` npmライブラリ、非公式API)
- Next.js Route Handler(`/api/...`)経由で取得し、クライアントから直接外部APIは叩かない
- クライアントは **SWRで60秒おきに再フェッチ**
- サーバー側でキャッシュ(30〜60秒程度のrevalidate)を挟み、レート制限を回避
- 全期間最高値: `range=max` のヒストリカルデータから算出。変動頻度が低いため長め(1日程度)にキャッシュ
- 市場休場・取得失敗時は直近の取得済み値を表示し、取得時刻(データの鮮度)を画面に表示する

## 5. 技術スタック・非機能要件

- Next.js(App Router)+ TypeScript
- SWR(60秒ポーリング)
- チャート: lightweight-charts
- デプロイ: Vercel。スマホでも閲覧するため**レスポンシブ対応必須**
- 認証: なし(URL非公開運用。`noindex`メタタグ推奨)
- DB: 不要(現在値のみ表示、履歴保存なし)

## 6. スコープ外(今回作らないもの)

- ポートフォリオ管理・資産配分・損益管理
- シグナル発生時の通知(ブラウザ通知・メール等)
- 指数やシグナルの履歴保存
- 銘柄追加・しきい値変更の管理UI