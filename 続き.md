実装完了です。仕様書の全機能を実装し、実データで検証しました。

## 実装したもの

**データ層** (`lib/`) — `config.ts`(銘柄・しきい値・基準値150の単一情報源)、`indicators.ts`(X/Y/指数の純関数)、`yahoo.ts`、`fx.ts`、`candles.ts`、`cache.ts`、`lastGood.ts`、`services/`

**API** — `/api/quotes`(ダッシュボード)、`/api/history/[symbol]`(足種4種)、`/api/health`(開発用)

**画面** — ダッシュボード(カード4枚+共通ヘッダー)、`/chart/[symbol]`(足種4×ライン/ローソク)

## 計画から変えた点(実データで判明)

- **FXの日付キーは取引所TZ基準に変更**。Yahoo の `USDJPY=X` は取引所TZが Europe/London で、夏時間中の日足が前日23:00Zに打たれます。UTC日付でキーを作ると米国株と **12.3%** ずれて円換算が systematically 1日古いレートになるところ、London基準で **0.5%** に下がりました(残りは FX が実際に休んでいる日で、前営業日補完が効きます)。
- **`/chart/[symbol]` に `dynamicParams = false`**。付けないと未知のslugでも静的シェルが HTTP 200 で返っていました(404に修正済み)。
- **鮮度表示は `useSyncExternalStore`**。当初の `useEffect`+`setState` は React Compiler の lint が正しく警告したため。
- `server-only` を1つ追加(Yahooクライアントのクライアント混入防止)。

## 検証結果

- 指数の不変条件: 全銘柄で `X ≤ 0` / `指数 = X+Y` / `シグナル = (指数 ≤ しきい値)` ✓
- 履歴 **4銘柄 × 4足種の全16通り**: OHLC整合(`low ≤ open,close ≤ high`)、時刻の厳密昇順・重複なし、有限値 ✓
- 3層フォールバック: キャッシュ有効時→キャッシュ / メモリ保持値あり→`stale`+旧取得時刻 / 全て空→503+理由 ✓
- クライアント: 通信断でも数値が消えず警告帯のみ追加 ✓
- チャート: 8通り全て例外なし、往復3回で canvas 積み上がりなし(破棄漏れなし)✓
- バンドル: チャートライブラリは両ページの初期HTMLに含まれず dynamic import のみ ✓
- `noindex` メタ + `robots.txt` ✓ / 型チェック・lint・本番ビルド ✓

## 判断が必要な点(Phase 3 で確認した実値)

|      | 現在値  | 最高値  | X       | 指数   | しきい値 |          |
| ---- | ------- | ------- | ------- | ------ | -------- | -------- |
| ACWI | $161.09 | $162.48 | −0.86%  | +5.84  | 0        | 待機     |
| INDY | $43.64  | $57.25  | −23.77% | −17.08 | −10      | **点灯** |
| SMH  | $553.11 | $668.91 | −17.31% | −10.62 | −20      | 待機     |
| AGG  | $97.49  | $119.63 | −18.51% | −11.82 | 0        | **点灯** |

事前に懸念していた **AGG の常時点灯は実測で確定** しました。最高値は2020/08/04で、価格が金利上昇後まだ戻っていないためです。参考までに、配当再投資ベース(`adjclose`)だと最高値は $99.97 になり X は −2.5%、指数 +4.2 で非点灯になります。

`lib/config.ts` に `ATH_BASIS_OVERRIDES`(銘柄ごとに基準を切替)と `threshold` を用意してあるので、`ATH_BASIS_OVERRIDES = { AGG: "adjclose" }` としきい値変更のどちらでも1行で対応できます。ご希望があれば入れます。

なお、`resize_window` がこの環境では viewport に効かなかったため、375px 相当の検証はカード幅を341pxに強制しての実測(4枚とも横はみ出し0)で行いました。ブレークポイント自体の目視確認はしていません。